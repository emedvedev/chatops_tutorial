---
description: Action that executes the "echo" Linux command on localhost.
enabled: true
entry_point: ''
runner_type: "run-local"
name: repeat
parameters:
  cmd:
    immutable: true
    default: 'echo {{ phrase }}'
  phrase:
    type: string
    required: true
