---
description: Action that executes an arbitrary Linux command on the localhost.
enabled: true
entry_point: ''
runner_type: "run-local"
name: repeat
parameters:
  cmd:
    immutable: true
    default: 'echo {{ phrase }}'
  phrase:
  	type: string
  	required: true